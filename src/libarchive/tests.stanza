
#use-added-syntax(tests)
defpackage libarchive/tests :
  import core

  import libarchive

deftest test-untar :
  val x = untar("/tmp/foo.tar", "/tmp/foo", verbose = true, dryrun = false)
  #EXPECT(true)
